describe("Express JSON Api Request Validator", () => {
    describe("Higher Order Function", () => {
        it("Should take in a request handler function and return a handler function", () => {
          throw new Error("Not Yet Implimented");
        });
        it("Should take a configuration object specifying the shape of the request json", () => {
          throw new Error("Not Yet Implimented");
        });
    });

    describe("Parameter Validation", () => {
        it("Should reject requests with bodies not matching the input config", () => {
          throw new Error("Not Yet Implimented");
        });
        it("Should accept requests with bodies that do match the input config", () => {
          throw new Error("Not Yet Implimented");
        });
    });
});
